@{
    ViewBag.Title = "用户权限";
}
@section Styles{

}
<div ng-app="app" ng-controller="userpermissionController">
    <br />
    <div class="ng-grid panel panel-default" ng-cloak>
        <div class="panel-heading">
            <b>用户权限</b>
            <a class="btn btn-primary pull-right" ng-click="gridOption.create();"><i class="fa fa-plus"></i>新 建</a>
        </div>
        <div class="panel-body">
            <div grid grid-option="gridOption" data="data" grid-name="userpermission"></div>
        </div>
    </div>  
</div>
@section scripts{
    <script type="text/javascript">
        app.controller("userpermissionController", [
            "$scope", "$http", "$filter", "$window", "$common", function ($scope, $http, $filter, $window, $common) {
                $scope.gridOption = {
                    height: 'auto',
                    rowNumber: true,
                    pkColumn: "Id",
                    rowDetail: true,
                    filterRow: true,
                    url: "/userpermission/page",
                    selectUrl: "/userpermission/select2",
                    editTemplateUrl: "/ngTemplate/userpermission.html"
                //, onRowSelected: function (row) { }
               // , onRenderRowDetail: function (row) { }
                };
            }
        ])
    </script>
}

